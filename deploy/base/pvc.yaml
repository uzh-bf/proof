apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: proof-sdk-data
  namespace: proof
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 10Gi

apiVersion: apps/v1
kind: Deployment
metadata:
  name: proof-sdk
  namespace: proof
spec:
  template:
    spec:
      containers:
        - name: proof-sdk
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 2Gi
